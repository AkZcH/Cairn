# ingest runs on your HOST machine (via `cargo run`), not inside Docker —
# so this connects through the host-mapped port, not the internal one.
DATABASE_URL=postgresql://cairn:changeme@localhost:5433/cairn